#include <stdio.h>

int main(void)
{
	int x = 50, y = 30;
	// x가 y의 같은 값을 가지고 있는가?
	// x - y -> x와 y가 같은 상황 그 자체를 의미.
	// x -- y -> true, x와 y가 같은 값을 가ㅣ고 있는 것.
	printf("x가 y와 같은가? %d\n", x ==y);
	printf("x가 y와 다른가? %d\n", x !=y);
	printf("x가 y와 큰가? %d\n", x > y);
	printf("x가 y와 작은가? %d\n", x < y);
	printf("x가 y값을 넣으면? %d\n", x = y);
	if 
	return 0;
}
